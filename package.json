{
  "name": "hepha-skill",
  "version": "2.0.0",
  "private": true,
  "description": "Hepha autonomous delivery skill runtime and local personal wiki",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
