---
description: Hepha 的 Spec、TDD、双审查、个人 Wiki、提交和停止硬约束。
alwaysApply: true
---

# Hepha Guard

仅在用户明确启用 Hepha、autopilot、自主循环或小步持续提交时应用。

## 硬门禁

1. 先写 Spec 和 Given/When/Then 验收条件，再执行代码任务。
2. `.hepha/backlog.json` 是任务状态的机器事实源，修改后必须运行 validator。
3. 一轮只执行一个 ready 任务，不改无关文件。
4. 行为变更必须留下有效 RED、GREEN 和 regression 证据。
5. Spec review 和 code review 必须分别通过。
6. UI/交互变更必须留下浏览器验证证据。
7. 每轮必须生成 Summary，并满足任务的个人资产策略。
8. 默认需要人工批准；只有 manifest 明确关闭时才允许无人值守完成。
9. 门禁未通过时禁止把任务标记为 `done` 或提交。

## 资产边界

- Summary 记录任务事实，Wiki 只保存可复用结论。
- 资产必须关联 Spec、Task、Test 和 Commit。
- 禁止发布密钥、token、个人信息和项目机密。
- `project-confidential` 资产禁止同步到跨项目个人 Wiki。

## 状态和失败

使用状态：`todo | doing | review | blocked | done | skipped`。`done`和`skipped`是终态。

第一次失败时诊断并重试。第二次相同失败时暂停实现并重新检查 Spec、任务边界和方案。只有没有可信替代路径时才报告阻塞。

## 提交

- 使用 conventional commit。
- 不允许空提交。
- 提交前检查工作树和敏感信息。
- 一个任务保持最小、可回滚的提交边界。
- 未完成 Summary、索引和资产策略时禁止提交。
