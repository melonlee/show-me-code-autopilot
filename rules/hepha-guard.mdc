---
description: Hepha 自主迭代交付的硬约束：中文记录、检查/审查门禁、每轮 summary、最小提交和安全停止。
alwaysApply: true
---

# Hepha Guard

本规则仅在用户明确要求 hepha、autopilot、自主循环、无人值守迭代或小步持续提交时生效。

## 循环契约

Hepha 激活后必须按以下顺序执行：

1. 计划
2. 研究
3. 执行
4. 检查
5. 审查
6. 沉淀
7. 提交

不要跳过或调换阶段。普通低风险任务可在“研究”阶段记录“不需要额外研究”。

## 中文记录

- 用户可读的任务、验收、进度、决策和 summary 默认使用中文。
- 代码标识符、命令、路径、错误输出保持原文。

## 每轮范围

- 每轮只处理一个最小 ready 任务。
- 优先控制在少量相关文件内。
- 如果范围变大，停止当前执行并重新拆分。
- 不触碰无关模块。

## 强制证据

提交前必须在 `.hepha/` 中留下证据：

1. 计划依据
2. 检查结果
3. 审查结果
4. 每轮 summary：`.hepha/summary/YYYY-MM-DD/<person>/TASK-XXX.md`

## 研究要求

遇到技术选型、不确定实现、架构变更或高风险决策时：

- 优先查项目内既有模式和官方资料。
- 至少比较两个方案。
- 在 `.hepha/decision-log.md` 记录证据、权衡和最终决定。

## 检查门禁

相关检查失败时：

- 先修复失败原因。
- 重新运行检查。
- 检查通过前不得提交。

## 审查门禁

UI 或交互变更必须做浏览器验证：

- 打开受影响页面或路由。
- 执行关键用户路径。
- 验证可见结果。
- 将步骤和证据记录到 `.hepha/progress.md` 与本轮 summary。

## 提交安全

- 一个 loop 对应一个 commit。
- 使用 conventional commit。
- 不允许空提交。
- 不提交密钥、token 或敏感凭据。
- summary 未生成时不得提交。

## 停止条件

满足以下任一条件时停止 Hepha 并报告：

1. 同一任务连续失败两次。
2. 没有 ready 任务且阻塞未解决。
3. 必需验证工具不可用且没有可信替代方案。
4. 继续执行会超过用户授权的风险边界。

报告必须包含：

- 阻塞详情
- 已尝试动作
- 当前 `.hepha` 产物状态
- 建议下一步
